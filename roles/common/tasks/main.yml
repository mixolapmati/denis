---
 - name: Apt-get update
   apt: update_cache=yes
 - name: Install common packages
   apt: name={{ item }} state=present
   with_items:
      - python-pip
      - python-dev
      - libmysqld-dev
      - git
